import 'package:flutter/material.dart';

/// Conjunto de colores para usar en la aplicación, y que no están definidos
/// en el [ColorScheme].
class MyColors {
  static const lightGrey = Color(0xFF95A1AC);
  static const lightGreyFocused = Color(0xFFDBE2E7);
}